<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rituka.uploadrecipe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.WebPageCall" />
        <activity android:name=".activities.UploadRecipe" />
        <activity android:name=".activities.SearchRecipes" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.FestivalsActivity" />
        <activity android:name=".activities.Search_a_profileActivity" />
        <activity android:name=".activities.ViewAdvisesActivity" />
        <activity android:name=".activities.ViewRecipesActivity" />
        <activity android:name=".activities.ShopMenuActivity" />
        <activity android:name=".activities.CheckoutActivity"></activity>
    </application>

</manifest>